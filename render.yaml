services:
  - type: worker
    name: forex-forecast-worker
    runtime: python
    buildCommand: |
      pip install --upgrade pip
      pip install --upgrade setuptools wheel
      pip install -r requirements.txt
    startCommand: python forecast_runner.py
    plan: free

